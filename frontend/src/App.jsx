import Dashboard from './pages/Dashboard.jsx'

/**
 * App.jsx — Root component.
 * Currently renders the single-page Dashboard directly.
 * Add a router here (e.g. react-router-dom) when additional pages are needed.
 */
export default function App() {
  return <Dashboard />
}
